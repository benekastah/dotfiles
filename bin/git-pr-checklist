#!/bin/sh

yesno() {
    read -p "$1 (y/n) " response
    case "$response" in
        y*|Y*) ;;
        *) exit 1
    esac
}

if [ ! -t 0 ]; then
    echo 'This script must be run interactively' >&2
    exit 1
fi

yesno 'Have you reviewed this code?'
yesno 'Is this code reviewable by others?'
